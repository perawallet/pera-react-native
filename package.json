{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint -- --fix",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "generate:api": "kubb generate && pnpm run lint && pnpm run format",
    "test": "turbo run test -- --coverage"
  },
  "devDependencies": {
    "@kubb/cli": "catalog:",
    "@kubb/core": "catalog:",
    "@kubb/plugin-msw": "catalog:",
    "@kubb/plugin-oas": "catalog:",
    "@kubb/plugin-react-query": "catalog:",
    "@kubb/plugin-ts": "catalog:",
    "@kubb/plugin-zod": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "turbo": "^2.5.6",
    "vitest": "catalog:"
  },
  "packageManager": "pnpm@10.15.1",
  "name": "with-vite"
}
