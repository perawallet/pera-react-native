# Pera Wallet Fastlane Environment Variables (Default Template)
#
# This file contains default/placeholder values for all environment variables
# used by Fastlane. It is committed to version control as documentation.
#
# For actual builds:
# - Copy this file to .env.staging or .env.production
# - Fill in the real values
# - DO NOT commit .env.staging or .env.production (they're gitignored)

# ============================================================================
# Apple Developer / iOS Configuration
# ============================================================================

# Apple ID (email) for App Store Connect
APPLE_ID=your-apple-id@example.com

# Apple Team ID (10-character alphanumeric)
APPLE_TEAM_ID=XXXXXXXXXX

# iOS Bundle Identifiers for each environment
IOS_DEV_BUNDLE_ID=com.algorandllc.perarn.dev
IOS_STAGING_BUNDLE_ID=com.algorandllc.perarn.staging
IOS_PRODUCTION_BUNDLE_ID=com.algorandllc.perarn

# ============================================================================
# Fastlane Match (Code Signing)
# ============================================================================

# Git repository URL for storing certificates and profiles
# This should be a PRIVATE repository
MATCH_GIT_URL=https://github.com/perawallet/pera-wallet-certificates

# Password to encrypt/decrypt certificates in the Match repository
# Generate a strong random password and store it securely
MATCH_PASSWORD=

# Optional: Use SSH instead of HTTPS for git operations
# MATCH_GIT_URL=git@github.com:perawallet/pera-wallet-certificates.git

# ============================================================================
# Android Configuration
# ============================================================================

# Android App IDs for each environment
ANDROID_DEV_APP_ID=com.algorandllc.perarn.dev
ANDROID_STAGING_APP_ID=com.algorandllc.perarn.staging
ANDROID_PRODUCTION_APP_ID=com.algorandllc.perarn

# Android Keystore Configuration
# Path to the release keystore file (relative to fastlane directory)
ANDROID_KEYSTORE_FILE=../apps/mobile/android/app/release.keystore

# Keystore password
ANDROID_KEYSTORE_PASSWORD=

# Key alias within the keystore
ANDROID_KEY_ALIAS=pera-wallet-release

# Key password (may be same as keystore password)
ANDROID_KEY_PASSWORD=

# ============================================================================
# CI/CD Configuration
# ============================================================================

# Set to 'true' when running in CI environment
CI=false

# GitHub Actions specific (automatically set by GitHub)
# GITHUB_ACTIONS=true

# ============================================================================
# App Store Connect / Play Store API Keys (for automated uploads)
# ============================================================================

# App Store Connect API Key ID
# ASC_KEY_ID=

# App Store Connect Issuer ID
# ASC_ISSUER_ID=

# Path to App Store Connect API Key (.p8 file)
# ASC_KEY_PATH=

# Google Play Store Service Account JSON path
# PLAYSTORE_JSON_KEY_PATH=

# ============================================================================
# Usage Instructions
# ============================================================================
#
# 1. For local development:
#    - No env file needed for dev builds (uses defaults)
#    - Run: pnpm --filter mobile ios (or android)
#
# 2. For staging builds locally:
#    - Copy this file to fastlane/.env.staging
#    - Fill in staging values
#    - Run: pnpm --filter mobile build:ios:staging
#
# 3. For production builds locally:
#    - Copy this file to fastlane/.env.production
#    - Fill in production values
#    - Run: pnpm --filter mobile build:ios:production
#
# 4. For CI/CD:
#    - Set all required values as repository secrets
#    - GitHub Actions will use these secrets automatically
#
# ============================================================================
# Security Notes
# ============================================================================
#
# - NEVER commit .env.staging or .env.production to git
# - Store sensitive values in a password manager
# - Use different API keys for staging and production
# - Rotate keys and passwords regularly
# - Limit access to Match certificate repository
# - Use read-only Match mode in CI (readonly: true)
#
