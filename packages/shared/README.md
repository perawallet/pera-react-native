# Pera Core

Base types, utilities and models used by the app.

## What lives here

- Query clients and HTTP utilities:
    - [createFetchClient()](packages/wallet-core-shared/src/api/query-client.ts:55) — network-aware ky wrapper
- Base types and models (packages/wallet-core-shared/src/models)
- Utilities in [packages/wallet-core-shared/src/utils](packages/wallet-core-shared/src/utils/index.ts)
