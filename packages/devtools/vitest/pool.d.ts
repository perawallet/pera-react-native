export declare const maxThreads: number

export declare const poolConfig: {
    pool: 'threads'
    poolOptions: {
        threads: {
            maxThreads: number
            minThreads: number
        }
    }
}
