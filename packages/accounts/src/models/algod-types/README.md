These files are placed here temproarily to provide a type defintion for algod REST response types. 

TODO: Remove these files when algod types are added to either algokit-utils OR we decide to keep using algod and we add these types more judiciously to the wallet-core-blockchain package