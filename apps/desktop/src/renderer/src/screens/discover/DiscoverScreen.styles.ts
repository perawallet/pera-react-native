import styled from 'styled-components'

export const DiscoverScreenContainer = styled.div`
  padding: 0 var(--spacing-xl);
  max-width: 96rem;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xl);
`
