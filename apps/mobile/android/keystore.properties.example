# Android Release Keystore Configuration
#
# This file contains the configuration for signing Android release builds.
# Copy this file to keystore.properties (which is gitignored) and fill in your values.
#
# IMPORTANT: Never commit keystore.properties or the actual keystore file to version control!

# Path to the release keystore file (relative to android/ directory)
storeFile=app/release.keystore

# Password for the keystore
storePassword=your-keystore-password-here

# Alias of the key within the keystore
keyAlias=pera-wallet-release

# Password for the key
keyPassword=your-key-password-here

# ============================================================================
# Generating a Release Keystore
# ============================================================================
#
# If you don't have a release keystore yet, generate one with:
#
# keytool -genkeypair -v \
#   -keystore app/release.keystore \
#   -alias pera-wallet-release \
#   -keyalg RSA \
#   -keysize 2048 \
#   -validity 10000
#
# You'll be prompted for:
# - Keystore password (store this securely!)
# - Key password (can be same as keystore password)
# - Your name, organization, etc. (for the certificate)
#
# ============================================================================
# Usage
# ============================================================================
#
# 1. Copy this file:
#    cp keystore.properties.example keystore.properties
#
# 2. Fill in your keystore details in keystore.properties
#
# 3. Place your release.keystore file in apps/mobile/android/app/
#
# 4. Build a release:
#    ./gradlew assembleProductionRelease
#
# ============================================================================
# Security Notes
# ============================================================================
#
# - Store keystore.properties and the keystore file securely
# - Use a password manager for passwords
# - Back up the keystore file in a secure location
# - If you lose the keystore, you cannot update the app on Play Store
# - Different keystores for staging and production is recommended
# - For CI/CD, store keystore and passwords as encrypted secrets
#
