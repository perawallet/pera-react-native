diff --git a/dist/storage/driver.d.ts b/dist/storage/driver.d.ts
index ca123fe2c6315a96902b6c70a85e96676fb98bf1..46b43926b9cded15e09424dfeb9af1ff66de3090 100644
--- a/dist/storage/driver.d.ts
+++ b/dist/storage/driver.d.ts
@@ -22,6 +22,12 @@ export interface IndexedDBDriverOptions {
     indexedDB?: IDBFactory;
     /** Database name; defaults to `"keystore"`. */
     databaseName?: string;
+    /**
+     * PERA PATCH: supplies the AES-GCM master key per operation. When set, the
+     * reserved `__keystore.master__` record is never read or written and
+     * `ready` only opens the database.
+     */
+    masterKey?: () => Promise<CryptoKey>;
 }
 /**
  * Creates a {@link KeyStoreDriver} backed by IndexedDB.
@@ -32,7 +38,7 @@ export interface IndexedDBDriverOptions {
  *
  * @param options - {@link IndexedDBDriverOptions}.
  * @returns A ready-to-use {@link KeyStoreDriver}. Its `ready` promise resolves
- *   once the database is open and the master key is available.
+ *   once the database is open and, without a `masterKey` provider, the master key is available.
  */
 export declare function createIndexedDBDriver(options: IndexedDBDriverOptions): KeyStoreDriver;
 //# sourceMappingURL=driver.d.ts.map
\ No newline at end of file
diff --git a/dist/storage/driver.js b/dist/storage/driver.js
index 4d187c3115a66936b1359b9a37188b99e2a5e406..6acaa0a86c7f107cf3dd3504ffbe55f0608b9cdd 100644
--- a/dist/storage/driver.js
+++ b/dist/storage/driver.js
@@ -51,12 +51,21 @@ export function createIndexedDBDriver(options) {
     const host = options.host;
     const factory = options.indexedDB ?? globalThis.indexedDB;
     const databaseName = options.databaseName ?? "keystore";
+    // PERA PATCH: an externally supplied master key, resolved on EVERY
+    // operation rather than captured in `ready`. A key captured once would
+    // outlive a lock, and a context that boots locked would either reject
+    // `ready` for good or hang every method behind it. With a provider the
+    // reserved record is neither read nor written.
+    const masterKey = options.masterKey;
     let db;
     let master;
     const ready = (async () => {
         db = await openDatabase(databaseName, factory);
-        master = await getMasterKey(db, host);
+        if (!masterKey) {
+            master = await getMasterKey(db, host);
+        }
     })();
+    const resolveMaster = () => (masterKey ? masterKey() : Promise.resolve(master));
     return {
         capabilities: CAPABILITIES,
         ready,
@@ -71,8 +80,15 @@ export function createIndexedDBDriver(options) {
                 });
                 return;
             }
-            const sealed = await seal(host, master, material.bytes);
-            material.bytes.fill(0);
+            let sealed;
+            try {
+                sealed = await seal(host, await resolveMaster(), material.bytes);
+            }
+            finally {
+                // PERA PATCH: the driver owns the buffer; a locked vault is a routine
+                // rejection here and must not leave the plaintext behind.
+                material.bytes.fill(0);
+            }
             await db.put(MATERIAL_STORE, { id, kind: "bytes", ...sealed });
         },
         async use(id, _ctx, fn) {
@@ -86,7 +102,7 @@ export function createIndexedDBDriver(options) {
                     publicKey: record.publicKey,
                 });
             }
-            const bytes = await open(host, master, record);
+            const bytes = await open(host, await resolveMaster(), record);
             try {
                 return await fn({ kind: "bytes", bytes });
             }
diff --git a/dist/storage/vault.js b/dist/storage/vault.js
index 7d8482c3814b01244931d5751bfabcc788cc2136..f2d1ea0b2c446eb6c856d75d25336f976752054c 100644
--- a/dist/storage/vault.js
+++ b/dist/storage/vault.js
@@ -30,16 +30,40 @@ export async function getMasterKey(db, subtle) {
     if (existing && existing.kind === "cryptokey") {
         return existing.privateKey;
     }
-    const master = await subtle.generateKey({ name: "AES-GCM", length: 256 }, false, [
-        "encrypt",
-        "decrypt",
-    ]);
-    await db.put(MATERIAL_STORE, {
-        id: MASTER_KEY_ID,
-        kind: "cryptokey",
-        privateKey: master,
-    });
-    return master;
+    // PERA PATCH: create under a cross-context lock, and re-read once held.
+    //
+    // Every context that builds a driver (service worker, offscreen document,
+    // each extension page) runs this concurrently at first use. Unpatched, all
+    // of them miss the read, all generate, and the last `put` wins — while each
+    // context keeps the key IT generated cached in the driver closure. Material
+    // sealed by one context then cannot be opened by another, surfacing as a
+    // bare `OperationError` from AES-GCM with no message.
+    //
+    // Web Locks are shared across every same-origin context including workers,
+    // so the first caller creates and the rest re-read what it wrote. Where the
+    // API is absent (non-browser test envs) the double-check alone still shrinks
+    // the window to nothing meaningful.
+    const createMasterKey = async () => {
+        const raced = await db.get(MATERIAL_STORE, MASTER_KEY_ID);
+        if (raced && raced.kind === "cryptokey") {
+            return raced.privateKey;
+        }
+        const master = await subtle.generateKey({ name: "AES-GCM", length: 256 }, false, [
+            "encrypt",
+            "decrypt",
+        ]);
+        await db.put(MATERIAL_STORE, {
+            id: MASTER_KEY_ID,
+            kind: "cryptokey",
+            privateKey: master,
+        });
+        return master;
+    };
+    const locks = globalThis.navigator && globalThis.navigator.locks;
+    if (!locks) {
+        return createMasterKey();
+    }
+    return locks.request("keystore-master-key-init", createMasterKey);
 }
 /**
  * Seals plaintext bytes with the master key (AES-GCM).
diff --git a/src/storage/driver.ts b/src/storage/driver.ts
index 588062b8da26a90d678d57b3a6349eabb3910c39..0a46cae722198c9221ad5ec5f1ba68101e4fde7a 100644
--- a/src/storage/driver.ts
+++ b/src/storage/driver.ts
@@ -30,7 +30,7 @@ import {
   METADATA_STORE,
   openDatabase,
 } from "./db.ts";
-import { getMasterKey, MASTER_KEY_ID, open, seal } from "./vault.ts";
+import { getMasterKey, MASTER_KEY_ID, open, seal, type SealedBytes } from "./vault.ts";
 
 /** Options for {@link createIndexedDBDriver}. */
 export interface IndexedDBDriverOptions {
@@ -40,6 +40,12 @@ export interface IndexedDBDriverOptions {
   indexedDB?: IDBFactory;
   /** Database name; defaults to `"keystore"`. */
   databaseName?: string;
+  /**
+   * PERA PATCH: supplies the AES-GCM master key per operation. When set, the
+   * reserved `__keystore.master__` record is never read or written and
+   * `ready` only opens the database.
+   */
+  masterKey?: () => Promise<CryptoKey>;
 }
 
 const CAPABILITIES: DriverCapabilities = {
@@ -73,20 +79,26 @@ function assertNotReserved(id: KeyId): void {
  *
  * @param options - {@link IndexedDBDriverOptions}.
  * @returns A ready-to-use {@link KeyStoreDriver}. Its `ready` promise resolves
- *   once the database is open and the master key is available.
+ *   once the database is open and, without a `masterKey` provider, the master key is available.
  */
 export function createIndexedDBDriver(options: IndexedDBDriverOptions): KeyStoreDriver {
   const host = options.host;
   const factory = options.indexedDB ?? globalThis.indexedDB;
   const databaseName = options.databaseName ?? "keystore";
 
+  // PERA PATCH: see the dist/ copy of this comment for the full rationale.
+  const masterKey = options.masterKey;
   let db!: KeyStoreDatabase;
   let master!: CryptoKey;
 
   const ready = (async (): Promise<void> => {
     db = await openDatabase(databaseName, factory);
-    master = await getMasterKey(db, host);
+    if (!masterKey) {
+      master = await getMasterKey(db, host);
+    }
   })();
+  const resolveMaster = (): Promise<CryptoKey> =>
+    masterKey ? masterKey() : Promise.resolve(master);
 
   return {
     capabilities: CAPABILITIES,
@@ -103,8 +115,14 @@ export function createIndexedDBDriver(options: IndexedDBDriverOptions): KeyStore
         });
         return;
       }
-      const sealed = await seal(host, master, material.bytes);
-      material.bytes.fill(0);
+      let sealed: SealedBytes;
+      try {
+        sealed = await seal(host, await resolveMaster(), material.bytes);
+      } finally {
+        // PERA PATCH: the driver owns the buffer; a locked vault is a routine
+        // rejection here and must not leave the plaintext behind.
+        material.bytes.fill(0);
+      }
       await db.put<MaterialRecord>(MATERIAL_STORE, { id, kind: "bytes", ...sealed });
     },
 
@@ -122,7 +140,7 @@ export function createIndexedDBDriver(options: IndexedDBDriverOptions): KeyStore
           publicKey: record.publicKey,
         });
       }
-      const bytes = await open(host, master, record);
+      const bytes = await open(host, await resolveMaster(), record);
       try {
         return await fn({ kind: "bytes", bytes });
       } finally {
diff --git a/src/storage/vault.ts b/src/storage/vault.ts
index 0289e1709d711f1fe477a06594dfbdd9bc745a2f..ed9c94f7d90d4d171363724c3381a8a9fc62faba 100644
--- a/src/storage/vault.ts
+++ b/src/storage/vault.ts
@@ -44,16 +44,29 @@ export async function getMasterKey(db: KeyStoreDatabase, subtle: SubtleCrypto):
   if (existing && existing.kind === "cryptokey") {
     return existing.privateKey;
   }
-  const master = await subtle.generateKey({ name: "AES-GCM", length: 256 }, false, [
-    "encrypt",
-    "decrypt",
-  ]);
-  await db.put<MaterialRecord>(MATERIAL_STORE, {
-    id: MASTER_KEY_ID,
-    kind: "cryptokey",
-    privateKey: master,
-  });
-  return master;
+  // PERA PATCH: create under a cross-context lock, and re-read once held.
+  // See the dist/ copy of this comment for the full rationale.
+  const createMasterKey = async (): Promise<CryptoKey> => {
+    const raced = await db.get<MaterialRecord>(MATERIAL_STORE, MASTER_KEY_ID);
+    if (raced && raced.kind === "cryptokey") {
+      return raced.privateKey;
+    }
+    const master = await subtle.generateKey({ name: "AES-GCM", length: 256 }, false, [
+      "encrypt",
+      "decrypt",
+    ]);
+    await db.put<MaterialRecord>(MATERIAL_STORE, {
+      id: MASTER_KEY_ID,
+      kind: "cryptokey",
+      privateKey: master,
+    });
+    return master;
+  };
+  const locks = globalThis.navigator?.locks;
+  if (!locks) {
+    return createMasterKey();
+  }
+  return locks.request("keystore-master-key-init", createMasterKey);
 }
 
 /**
